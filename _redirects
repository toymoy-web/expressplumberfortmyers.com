# Intentionally empty. Cloudflare Pages resolves clean URLs and slug/index.html
# routing natively. Do not add 200/301 rules here unless there is a genuine
# cross-domain or legacy-path redirect need (see site-build-qa Section 4).
